class AuthService {
  public signup = async () => {
    return { message: 'signup route' };
  };

  public login = async () => {
    return { message: 'login route' };
  };
}

export default AuthService;
